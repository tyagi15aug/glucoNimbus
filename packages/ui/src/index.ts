/**
 * Placeholder. `apps/web/components/` holds the only two UI components
 * that exist today (`GlucoseChart`, `LiveDashboard`) — nothing to share
 * across apps yet. Move a component here the first time `apps/simulator`
 * (its own dev-only web panel, if that gets built — spec Section 5's
 * "developer-only simulator panel" is currently just the CLI's terminal
 * output) or a future app needs it too.
 */
export {};
